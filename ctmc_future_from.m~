function [p] = ctmc_future_from(i, Q, t)
    h = 1e-3;
    n_steps = ceil(t / h);

    p = zeros(1,size(Q,2));
    p(i) = 1;
    p = Q_step^
    Q_step = eye(size(Q,2)) + Q*h;
    
    for k = 1:n_steps
        p = p * Q_step;
    end
end